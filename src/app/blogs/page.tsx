import BlogHeroSection from "@/components/blogs-page-components/BlogHeroSection";
import React from "react";

const page = () => {
  return (
    <div>
      <BlogHeroSection />
    </div>
  );
};

export default page;
